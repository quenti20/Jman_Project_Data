version: 2

models:
  - name: training
    columns:
      - name: worksession_id
      tests:
        - unique
        - not_null
      # tests:
      #   - unique:
      #       columns: worksession_id
      
